{
  "name": "folder-backup-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend local para Folder Backup Agent — API para o celular e webhooks para n8n",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file=.env src/index.js",
    "dev": "node --env-file=.env --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^13.10.0",
    "multer": "^1.4.5-lts.1",
    "uuid": "^11.0.3"
  }
}
